# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{showoff\.json})
  watch(%r{.+\.css})
  watch(%r{one/.+\.md})
  watch(%r{tmp/restart\.txt})
end
